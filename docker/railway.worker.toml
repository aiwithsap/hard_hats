# Railway configuration for Worker Service
# Place this in the root directory when deploying the worker service

[build]
builder = "DOCKERFILE"
dockerfilePath = "docker/Dockerfile.worker"

[deploy]
# No healthcheck - worker doesn't expose HTTP
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
